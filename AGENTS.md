# AGENTS.md — 개인 프로젝트 개발 원칙

> 이 문서는 이 저장소에서 작업하는 개발자와 AI 코딩 에이전트가 따르는 단일 기준이다.
> 프로젝트의 목표는 단순한 기능 구현이 아니라, 대용량 배치 조회 전략의 차이를 공정하게 측정하고 재현 가능한 포트폴리오로 남기는 것이다.
> 아직 존재하지 않는 파일이나 구현 위치는 `(예정)`으로 표시하고, 실제 구현 시 경로를 갱신한다.

## 1. 프로젝트 정체성

### 한 줄 소개

Spring Batch에서 `LIMIT-OFFSET` 기반 페이징과 `WHERE id > :lastId` 기반 Zero Offset(Keyset Pagination)을 동일 조건으로 비교하는 성능 실험 프로젝트다.

### 왜 이 프로젝트인가

- 원문 사례의 `QueryDslZeroOffsetItemReader`, `ExposedCursorItemReader`는 이 프로젝트의 학습 범위 밖이므로 사용하지 않는다.
- Zero Offset Reader는 Spring Data JPA와 JPQL 또는 Criteria API만으로 직접 구현한다.
- Kotlin 전용 Exposed는 사용하지 않는다.
- 1억 건 환경을 과장해 재현했다고 주장하지 않는다. 로컬에서 가능한 10만·50만·100만 건 데이터셋으로 축소하여 상대적 성능과 증가 추세를 관찰한다.
- 카카오페이 정산 시스템 사례와 원문 수치는 문제의 배경일 뿐, 이 프로젝트의 실측 결과와 구분한다.

### 검증할 가설

1. `JpaPagingItemReader`의 후반 페이지는 offset 증가로 조회 비용이 커진다.
2. `id > lastId ORDER BY id ASC` 방식은 페이지가 진행되어도 조회 시작점을 인덱스로 찾을 수 있어 페이지별 성능이 더 일정하다.
3. 동일 환경에서 데이터 규모가 증가할수록 두 방식의 실행 시간 증가 양상이 달라진다.
4. 적절한 인덱스 유무는 조회 전략의 성능과 실행 계획에 유의미한 영향을 준다.
5. Reader별 메모리 사용 패턴은 구현과 JPA 영속성 컨텍스트 관리 방식에 따라 달라진다.

위 내용은 결론이 아니라 실험 전 가설이다. 구현 후 측정 결과가 가설과 다르면 결과를 수정하거나 숨기지 않고 원인을 분석한다.

## 2. 기술 범위와 제약

### 현재 기준 기술

- Java 21
- Spring Boot 4.1.0
- Spring Batch
- Spring Data JPA
- MySQL
- Gradle
- JUnit

버전은 `build.gradle`을 최종 기준으로 삼으며, 이 문서와 다르면 `build.gradle`에 맞게 이 절을 갱신한다.

### 허용

- JPQL 또는 Criteria API를 이용한 Zero Offset 구현
- Spring Batch가 제공하는 기본 Reader와 Job/Step 구성
- MySQL `EXPLAIN` 또는 `EXPLAIN ANALYZE`
- JVM GC 로그, Spring Batch 메타데이터, Actuator 등 측정에 필요한 도구
- 데이터 생성을 위한 SQL, 배치 INSERT, `LOAD DATA INFILE`

### MVP에서 제외

- QueryDSL 및 `QueryDslZeroOffsetItemReader`
- Kotlin Exposed 및 `ExposedCursorItemReader`
- 학습 목표를 가리는 외부 Zero Offset 라이브러리
- 분산 배치, 파티셔닝, 멀티스레드 Step을 이용한 성능 개선
- 운영 환경과 동등하다는 주장 또는 1억 건 처리 성능의 추정

새 라이브러리는 기존 기술로 해결하기 어려운 이유를 먼저 기록한 뒤 추가한다. 편의를 위한 의존성 추가로 핵심 구현을 대체하지 않는다.

## 3. 도메인과 핵심 흐름

### 최소 도메인 모델

실제 엔티티가 만들어지면 아래 예시를 실제 필드와 경로로 교체한다.

```text
Settlement(
  id,
  merchantId,
  status,
  amount,
  settledAt,
  createdAt,
  ...성능 실험에 필요한 조회 컬럼
)
```

### 배치 흐름

```text
데이터셋 생성
  → 동일 조회 조건 준비
  → Reader 유형 선택
  → chunk/page/fetch 크기 고정
  → 전체 데이터 읽기
  → 처리 건수 검증
  → 시간·메모리·실행 계획 기록
  → 3회 반복 결과 요약
```

### 비교 대상

| Reader | 조회 방식 | 구현 위치 |
| --- | --- | --- |
| `JpaPagingItemReader` | `LIMIT-OFFSET` 기반 페이지 조회 | `src/main/java/com/example/settlementbatchperformance/job/PagingSettlementJobConfig.java` |
| `ZeroOffsetItemReader` | `WHERE id > :lastId ORDER BY id ASC` | `src/main/java/com/example/settlementbatchperformance/job/ZeroOffsetItemReader.java` |
| `JpaCursorItemReader` | 메모리 특성 확인용 선택 실험 | (예정) |

`JpaCursorItemReader`의 OOM은 확정 사실로 단정하지 않는다. 제한된 힙과 동일 데이터셋에서 직접 관찰할 때만 이 프로젝트의 결과로 기록한다.

## 4. 구현 원칙

### 패키지 책임

- `config` 또는 `job`: Job, Step, Reader, Writer 설정
- `domain`: JPA Entity와 도메인 규칙
- `repository`: 데이터 접근과 실험용 조회
- `service`: 데이터 생성 또는 실험 실행 흐름이 필요할 때만 사용
- `support` 또는 `metrics`: 시간, 처리 건수, 메모리 등 측정 지원

현재 구조에 맞는 일관된 이름을 선택하며, 같은 책임을 여러 패키지에 흩뜨리지 않는다.

### Zero Offset Reader 불변 조건

- 정렬 기준은 유일하고 불변인 키여야 한다. MVP에서는 증가하는 PK `id`를 사용한다.
- 쿼리는 반드시 `WHERE id > :lastId ORDER BY id ASC`를 포함한다.
- 한 페이지를 정상적으로 처리한 뒤 마지막으로 읽은 `id`를 다음 조회 기준으로 갱신한다.
- 재시작을 지원한다면 `lastId`를 `ExecutionContext`에 저장하고 복원한다.
- Reader의 상태를 공유하지 않는다. 상태를 가진 Reader는 Step scope를 기본으로 검토한다.
- 빈 결과가 나오면 종료하며 같은 페이지를 무한 반복하지 않는다.
- 데이터 누락과 중복 여부를 전체 처리 건수 및 ID 검증 테스트로 확인한다.
- 실험 도중 대상 행의 PK가 변경되거나 삽입·삭제되는 조건은 기본 실험에서 제외한다.

### JPA 규칙

- 연관관계는 LAZY를 기본으로 한다.
- 연관 엔티티를 실제로 읽는 경우에만 fetch join 또는 EntityGraph를 검토한다.
- 컬렉션 fetch join과 페이징을 함께 사용해 메모리 페이징이 발생하지 않도록 한다.
- 성능 비교에서는 두 Reader가 동일한 엔티티/DTO와 동일한 컬럼을 읽어야 한다.
- 페이지 또는 청크 처리 후 영속성 컨텍스트가 불필요하게 누적되지 않는지 확인한다.
- N+1 개선 전후는 쿼리 수 또는 로그로 검증하고 증거를 남긴다.

### 트랜잭션과 오류 처리

- 트랜잭션 경계는 Step의 chunk 트랜잭션을 우선한다.
- Controller에 `@Transactional`을 붙이지 않는다.
- 성능 수치를 좋게 보이게 하려고 예외, skip, retry를 숨기지 않는다.
- 재시도 정책은 일시적 오류에만 적용하고 최대 횟수와 대상 예외를 명시한다.
- 실패한 실행도 원인과 함께 보존하여 성공 실행과 구분한다.

## 5. 공정한 성능 실험 규칙

### 고정 조건

- 기본 데이터셋: 100,000 / 500,000 / 1,000,000건
- 기본 `chunkSize`: 1,000
- 기본 `pageSize`: 1,000
- 기본 `fetchSize`: 1,000
- 조회 조건, 정렬, 반환 컬럼 수, Writer 동작을 Reader 간 동일하게 유지한다.
- 동일한 Docker MySQL 이미지, 스키마, JVM 옵션, 애플리케이션 빌드를 사용한다.
- 실행 중 다른 고부하 작업을 피하고 머신 사양을 기록한다.

조건을 바꾼 실험은 별도 실험으로 취급하며 한 번에 하나의 변수만 변경한다.

### 반복과 순서

- 각 조건은 워밍업 실행과 측정 실행을 구분한다.
- 측정은 최소 3회 반복하고 개별값과 평균을 모두 남긴다. 가능하면 중앙값도 함께 사용한다.
- 캐시 영향을 통제하기 위해 실행 순서를 교차하거나 무작위화하고 순서를 기록한다.
- DB 버퍼 풀, OS 캐시, JIT 영향을 제거하지 못했다면 한계로 명시한다.
- 실패·이상치를 임의로 제외하지 않는다. 제외 시 기준과 사유를 기록한다.

### 측정 지표

- Job 및 Step 시작/종료 시각과 실행 시간(ms)
- `readCount`, `writeCount`, `skipCount`, `rollbackCount`
- 데이터셋별 Reader 실행 시간과 상대 비율
- 데이터가 5배, 10배 증가할 때 실행 시간 증가 배율
- 페이지 번호 또는 처리 구간별 조회 시간(가능하면 수집)
- GC 로그의 최대 힙/Old Gen 사용량과 GC 횟수·정지 시간
- 인덱스 ON/OFF의 실행 계획과 실제 실행 시간

처리 성능은 가능하면 Reader 시간과 전체 Step 시간을 구분한다. Writer가 병목이면 Reader 비교 결과로 단정하지 않는다.

### 인덱스 실험 주의

- `id`가 PK이면 해당 인덱스를 단순히 OFF하거나 제거하지 않는다.
- 기본 Zero Offset 실험은 PK 인덱스가 존재하는 현실적인 조건에서 수행한다.
- 인덱스 ON/OFF 비교는 `status`, `merchant_id`, `settled_at` 등 동일 WHERE 조건에 쓰는 보조 컬럼 또는 복합 인덱스를 대상으로 한다.
- 비교할 인덱스 이름, DDL, 데이터 분포, 카디널리티를 기록한다.
- `EXPLAIN` 결과의 access type, key, rows, filtered를 저장한다. 지원되는 환경에서는 `EXPLAIN ANALYZE`의 실제 시간도 저장한다.
- 인덱스 변경 후 통계와 캐시 영향이 결과에 미칠 수 있음을 기록한다.

### 결과의 정직성

- 원문 수치인 하루 1억 건, offset 5천만의 지연, 300만 건 기준 6752초 대 266초는 `출처의 참고 수치`로만 표시한다.
- 이 프로젝트에서 직접 실행하지 않은 값을 자체 결과처럼 쓰지 않는다.
- 예측값, AI 생성값, 한 번만 실행한 값을 최종 성능 근거로 사용하지 않는다.
- 기대와 다른 결과도 원본 로그와 함께 공개한다.
- 그래프의 축, 단위, 표본 수, 집계 방식이 보이도록 작성한다.

## 6. 테스트와 완료 조건

### 필수 테스트

- 빈 테이블에서 정상 종료
- 데이터가 page size보다 작은 경우
- 데이터가 page size와 정확히 같은 경우
- 여러 페이지에 걸친 전체 조회
- ID 사이에 결번이 있는 경우
- 모든 ID가 한 번씩만 처리되는지 검증
- Job 실패 후 재시작 시 누락·중복 검증(재시작을 구현한 경우)
- 두 Reader가 동일 조건에서 동일 건수를 처리하는지 검증

성능 테스트와 정합성 테스트를 분리한다. 빠른 자동 테스트는 작은 데이터셋을 사용하고 10만 건 이상 실험은 별도 실행 절차로 둔다.

### 기능 완료(Definition of Done)

- 요구사항에 맞는 코드와 테스트가 함께 존재한다.
- 관련 테스트와 빌드가 통과한다.
- 전체 처리 건수가 원본 데이터 건수와 일치한다.
- 실행 방법과 필요한 환경 변수가 문서화되어 있다.
- 측정 작업이라면 원본 로그, 실행 환경, 반복 횟수, 요약표가 함께 남아 있다.
- 새로운 기술 결정이나 실험 조건 변경이 문서에 반영되어 있다.
- 비밀 정보와 로컬 전용 파일이 커밋 대상에 포함되지 않는다.

## 7. 2주 MVP 실행 계획

| Day | 목표 | 완료 증거 |
| --- | --- | --- |
| 1 | 요구사항 정리, ERD·스키마 설계, 10만 건 생성 | DDL, 생성 스크립트, 건수 검증 |
| 2 | Spring Batch 기본 구성, Paging Job 구현 | Job 실행 로그, 테스트 |
| 3 | Zero Offset Reader 구현 | 정합성 단위/통합 테스트 |
| 4 | 50만·100만 건 생성 및 1차 측정 | 데이터 생성 로그, 원본 측정 로그 |
| 5 | 보조 인덱스 ON/OFF 실험 | DDL, EXPLAIN 결과 |
| 6 | 결과 정리와 그래프 작성 | CSV/스프레드시트, 그래프 |
| 7 | GC 로그 및 힙 관찰, Cursor 선택 실험 | JVM 옵션, GC 로그, 관찰 기록 |
| 8 | 리팩터링, 예외·재시작 검증, 셀프 리뷰 | 테스트 결과, 리뷰 체크리스트 |
| 9 | 최종 재측정, README 재현 가이드 | 3회 반복 원본 데이터, README |
| 10 | 포트폴리오 스토리와 데모 준비 | PAAR 정리, 발표 자료 또는 데모 시나리오 |

일정이 밀리면 Cursor/OOM 실험과 시각화 고도화를 먼저 제외한다. 두 핵심 Reader의 정합성, 공정한 측정, 재현 문서는 제외하지 않는다.

## 8. 포트폴리오 산출물

최종 저장소에는 다음 증거를 남긴다.

- `JpaPagingItemReader`와 직접 구현한 `ZeroOffsetItemReader` 소스 및 테스트
- 10만·50만·100만 건 데이터 생성 스크립트
- 실험 환경과 재현 명령이 포함된 `README.md`
- Reader별 원본 실행 로그와 요약 CSV 또는 스프레드시트
- 데이터 건수 대비 실행 시간 그래프
- 보조 인덱스 ON/OFF DDL과 `EXPLAIN` 결과
- GC 로그 또는 힙 측정 결과
- 주요 판단과 트레이드오프를 정리한 `DECISIONS.md` (예정)

### PAAR 서술 기준

- **Problem**: OFFSET이 커질수록 앞 행을 탐색하고 버리는 비용이 증가하는 대량 배치 조회 문제
- **Analyze**: Paging, Zero Offset, JDBC Cursor, JPA Cursor의 구현 난이도·성능·메모리·학습 범위 비교
- **Action**: 동일 조건의 두 Job 구현, 반복 측정, 실행 계획과 GC 로그 수집
- **Result**: 직접 측정한 수치, 증가 배율, 정합성, 한계 및 다음 개선점

Result에는 구현 전 예상 문장을 넣지 않는다. 실제 측정이 끝난 뒤 숫자와 실험 조건을 함께 작성한다.

## 9. 개인 프로젝트 작업 방식

### 이슈와 변경 단위

- 한 작업은 하나의 명확한 목적만 가진다.
- 구현 전에 완료 조건과 검증 방법을 짧게 적는다.
- 큰 기능은 Reader 구현, 데이터 생성, 측정 자동화, 문서화처럼 검증 가능한 단위로 나눈다.
- 포트폴리오 설명에 필요한 판단 근거는 코드에만 남기지 말고 문서화한다.

### 셀프 리뷰 체크리스트

- 요구사항 밖의 기능을 추가하지 않았는가?
- 두 Reader의 조건이 정말 동일한가?
- 누락 또는 중복 데이터를 검증했는가?
- 테스트가 구현 세부가 아닌 동작을 검증하는가?
- 측정값에 환경과 반복 조건이 포함되어 있는가?
- 원문 수치와 자체 실측값이 명확히 구분되는가?
- 로그, 비밀 정보, 생성 데이터가 불필요하게 커밋되지 않는가?

## 10. Git 규칙

### 브랜치

- `main`은 언제든 실행 가능한 상태로 유지한다.
- 기능 단위 브랜치를 사용한다: `feat/zero-offset-reader`, `test/reader-consistency`, `docs/experiment-guide`.
- 개인 프로젝트이므로 `develop` 브랜치와 형식적인 승인 절차는 강제하지 않는다.
- 큰 변경은 가능하면 PR을 만들어 변경 목적과 실험 증거를 한곳에 남긴다.

### 커밋

- `<type>: <summary>` 형식을 사용한다.
- type은 `feat`, `fix`, `refactor`, `test`, `docs`, `perf`, `chore` 중 하나를 사용한다.
- 서로 다른 목적의 변경은 별도 커밋으로 나눈다.
- 커밋 전 관련 테스트를 실행하고 변경 파일을 직접 확인한다.
- 비밀 정보, IDE 설정, 대용량 생성 데이터, 불필요한 빌드 결과를 커밋하지 않는다.

### AI 에이전트 Git 제한

- 사용자의 명시적 요청 없이 commit, push, merge, rebase, 브랜치 삭제 또는 PR 생성을 수행하지 않는다.
- 작업 전 기존 파일과 사용자 변경사항을 확인하고 임의로 되돌리거나 덮어쓰지 않는다.
- force push와 파괴적 Git 명령은 수행하지 않는다.
- 충돌이나 의도 불명확한 변경은 임의로 결정하지 않고 사용자에게 알린다.

## 11. AI 코딩 에이전트 규칙

- 작업 전 이 문서와 현재 프로젝트 구조를 확인한다.
- 요청 범위 안에서만 수정하고 불필요한 구조 개편이나 의존성 추가를 피한다.
- 코드를 변경하면 가능한 범위에서 테스트 또는 빌드로 검증한다.
- 실행하지 않은 테스트를 통과했다고 말하지 않는다.
- 측정하지 않은 성능 향상률, 처리량, 메모리 사용량을 만들어내지 않는다.
- 성능 관련 변경은 정합성 보존을 먼저 검증한다.
- 구현 선택이 실험 공정성에 영향을 주면 그 사실을 보고하고 문서에 반영한다.
- 기존 사용자 변경사항과 무관한 파일을 수정하지 않는다.
- 비밀번호, DB 접속 정보, 토큰 등 민감 정보를 코드나 문서에 기록하지 않는다.
- 응답에는 변경 결과, 검증 결과, 남은 한계를 간결하게 제시한다.

## 12. 변경 절차

- 실험 가설, 고정 조건, 데이터셋, 핵심 기술 범위를 바꾸면 변경 이유와 영향을 `DECISIONS.md`에 기록한다.
- 기존 결과를 새 결과로 덮어쓰지 않고 날짜 또는 실행 ID로 구분하여 보존한다.
- 실제 코드 경로가 정해지면 이 문서의 `(예정)` 표시를 갱신한다.
- 문서와 구현이 충돌하면 무조건 한쪽을 따르지 말고 현재 요구사항을 확인한 뒤 둘을 함께 정정한다.
