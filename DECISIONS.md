# Decisions

성능 결과의 해석에 영향을 주는 구현·실험 결정을 날짜별로 기록한다. 측정 조건을 변경한 경우 기존 결과를 덮어쓰지 않고 새 실행 ID를 사용한다.

## 2026-08-13 — 10만 건 1차 성능 비교 기준

### 결정

- 두 Reader 모두 데이터 100,000건, chunk/page/fetch size 1,000, `id ASC`, 동일 엔티티, 동일 no-op Writer를 사용한다.
- 워밍업은 Reader별 1회 수행하고 측정은 Reader별 3회 수행한다.
- 측정 순서는 Paging→Zero Offset, Zero Offset→Paging, Paging→Zero Offset으로 교차한다.
- Reader 비교의 주 지표는 Step 실행 시간으로 정하고 Job 시간과 실제 호출 벽시계 시간도 원본 로그에 함께 보존한다.
- 페이지별 조회 시간은 page 0~99를 결과 반환 페이지로 집계한다. page 100은 종료 확인을 위한 빈 조회로 원본 로그에는 남기되 추세에서는 제외한다.

### 이유와 영향

Job 시작·종료 부가 비용보다 Reader가 포함된 Step을 비교하기 위해 Step 실행 시간을 주 지표로 선택했다. 페이지 로그를 추가하면 절대 시간에 측정 오버헤드가 포함되므로 두 Reader 모두 동일 형식으로 기록하고 이 한계를 결과에 명시한다.

## 2026-08-13 — 페이지 조회 트랜잭션 조건 정렬

### 결정

`JpaPagingItemReader`의 기본 동작과 맞추기 위해 `ZeroOffsetItemReader`도 각 페이지 JPQL 조회를 resource-local JPA 트랜잭션 안에서 실행한다. 조회가 끝난 뒤 영속성 컨텍스트를 비워 이전 페이지 엔티티가 누적되지 않게 한다.

### 이유와 영향

한 Reader만 페이지별 트랜잭션 비용을 부담하는 비교를 피하기 위한 결정이다. 이 선택은 Zero Offset의 절대 실행 시간에 영향을 줄 수 있으므로 최종 측정 전에 기준을 확정하고 소스와 함께 기록한다.

## 2026-08-13 — 재현 환경과 데이터 격리

### 결정

- 최종 10만 건 측정은 Java 21.0.7, Docker MySQL 8.4.10, Apple M5 10코어·16GiB 환경에서 수행한다.
- 성능 스크립트는 기본 개발 DB와 분리된 `settlement_benchmark_100k`를 사용한다.
- 페이지 실행 식별 정보가 없고 Java 26에서 수행된 `20260813-100k-baseline-01`은 형식 확인용으로만 보존하고 결과 집계에서 제외한다.
- 최종 실행 ID는 `20260813-100k-baseline-02`로 기록한다.

### 이유와 영향

프로젝트 기준인 Java 21을 지키고 기존 개발 데이터를 변경하지 않으면서 동일 데이터셋을 재생성하기 위한 선택이다. 두 실행을 섞지 않으므로 최종 요약값은 baseline-02의 측정 6개만 사용한다.

## 2026-08-13 — 50만·100만 건 규모 확장

### 결정

- 10만 건과 동일하게 chunk/page/fetch size 1,000, `id ASC`, 동일 엔티티와 no-op Writer를 사용한다.
- 50만 건은 `20260813-500k-baseline-01`, 100만 건 최종 결과는 `20260813-1m-baseline-02`를 사용한다.
- 데이터 크기만 바꾸기 위해 공통 스크립트는 `100000`, `500000`, `1000000`만 허용한다.
- 측정 시점의 커밋되지 않은 소스도 재현할 수 있도록 상태, diff, 소스 스냅샷을 결과 디렉터리에 함께 보존한다.

### 결과와 영향

- 10만 대비 50만 건 평균 시간은 Paging 12.93배, Zero Offset 4.85배였다.
- 10만 대비 100만 건 평균 시간은 Paging 87.86배, Zero Offset 10.56배였다.
- 이번 환경에서는 데이터 규모가 증가할수록 두 방식의 실행 시간 증가 양상이 달라진다는 가설과 일치했다.
- 이 수치는 단일 로컬 머신 결과이며 1억 건 운영 환경으로 확대해 해석하지 않는다.

## 2026-08-13 — 100만 건 시계 이상치와 재측정

### 관찰

`20260813-1m-baseline-01`의 Paging 측정 2회차에서 Step 시간은 511,195.758ms, 같은 실행의 `System.nanoTime()` 기반 경과 시간은 118,807.568ms였다. 페이지별 단조 시계 합계도 113,050.750ms로 후자와 비슷했다.

### 결정

- 처리 건수는 정상이지만 Step 시작·종료 시각의 무결성이 깨졌으므로 첫 실행 전체를 최종 3회 통계로 사용하지 않는다.
- 첫 실행과 이상치를 삭제하지 않고 원본 로그와 사유를 보존한다.
- 같은 고정 조건으로 전체 실행을 다시 수행하고 시계가 정상인 `20260813-1m-baseline-02`를 최종 통계에 사용한다.

### 영향

이상치 한 값만 선택적으로 제거하지 않고 실행 세트 단위로 재측정했다. 통합 결과표에는 첫 실행도 별도 이상치 시트로 남겨 제외 기준을 검토할 수 있게 했다.
